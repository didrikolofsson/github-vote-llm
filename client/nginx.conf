server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location /assets/ {
        try_files $uri =404;
    }

    location /board/ {
        try_files $uri /board.html;
    }

    location / {
        try_files $uri /index.html;
    }
}
